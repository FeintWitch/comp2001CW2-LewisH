import pymssql##main##
###I think this will work. lets hope so.###

try: 
    connection = pymssql(
    server = 'COMP2001sqlserv',
    database = 'cw2',
    trusted = 'yes'
    )

    print("Connected to the database")
except Exception as e:
    print("Error connecting to the database: ", e)
    